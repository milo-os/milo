apiVersion: quota.miloapis.com/v1alpha1
kind: ClaimCreationPolicy
metadata:
  name: project-configmap-claim-policy
  labels:
    app.kubernetes.io/name: milo
    app.kubernetes.io/component: quota-system
spec:
  trigger:
    resource:
      apiVersion: v1
      kind: ConfigMap
  target:
    resourceClaimTemplate:
      metadata:
        name: "configmap-{{ trigger.metadata.name }}"
        namespace: "{{requestInfo.namespace}}"
        labels:
          app.kubernetes.io/name: milo
          app.kubernetes.io/component: quota-system
        annotations:
          kubernetes.io/description: "Automatic quota claim for ConfigMap creation"
      spec:
        requests:
          - resourceType: core.miloapis.com/configmaps
            amount: 1
