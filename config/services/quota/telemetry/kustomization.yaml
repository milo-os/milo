apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# Kustomize component for quota system telemetry
# Includes both raw metrics collection and aggregation policies
# for comprehensive observability of the quota system.

metadata:
  name: quota-telemetry
  annotations:
    config.kubernetes.io/function: |
      container:
        image: registry.k8s.io/kustomize/kustomize:v5.0.1
    config.kubernetes.io/local-config: "true"

components:
  - metrics

generatorOptions:
  disableNameSuffixHash: false
  labels:
    milo.quota/telemetry: complete
    app.kubernetes.io/part-of: milo
    app.kubernetes.io/component: quota-system
    app.kubernetes.io/managed-by: kustomize
