apiVersion: iam.miloapis.com/v1alpha1
kind: Role
metadata:
  name: notification-contact-admin
  annotations:
    kubernetes.io/display-name: Notification Contact Admin
    kubernetes.io/description: Full access to notification contacts
spec:
  launchStage: Beta
  inheritedRoles:
    - name: notification-contact-editor
